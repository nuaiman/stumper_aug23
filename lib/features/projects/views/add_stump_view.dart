import 'package:flutter/material.dart';

class AddStumpView extends StatelessWidget {
  const AddStumpView({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Add Stump'),
      ),
    );
  }
}
